<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synapsetest.testmanagement.mapper.ResourcePoolMapper">

    <!-- Result Map for ResourcePool -->
    <resultMap id="ResourcePoolResultMap" type="com.synapsetest.testmanagement.model.ResourcePool">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="type" column="type"/>
        <result property="capacity" column="capacity"/>
        <result property="allocated" column="allocated"/>
        <result property="location" column="location"/>
        <result property="config" column="config" typeHandler="com.synapsetest.testmanagement.config.JsonTypeHandler"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- Select by ID -->
    <select id="selectById" resultMap="ResourcePoolResultMap">
        SELECT * FROM resource_pools WHERE id = #{id}
    </select>

    <!-- Select by Name -->
    <select id="selectByName" resultMap="ResourcePoolResultMap">
        SELECT * FROM resource_pools WHERE name = #{name}
    </select>

    <!-- Select by Status -->
    <select id="selectByStatus" resultMap="ResourcePoolResultMap">
        SELECT * FROM resource_pools WHERE status = #{status}
    </select>

    <!-- Select by Type -->
    <select id="selectByType" resultMap="ResourcePoolResultMap">
        SELECT * FROM resource_pools WHERE type = #{type}
    </select>

    <!-- Select by Status and Type -->
    <select id="selectByStatusAndType" resultMap="ResourcePoolResultMap">
        SELECT * FROM resource_pools WHERE status = #{status} AND type = #{type}
    </select>

    <!-- Insert -->
    <insert id="insert" parameterType="com.synapsetest.testmanagement.model.ResourcePool">
        INSERT INTO resource_pools(
            id, name, type, status, capacity, allocated, location,
            config, created_at, updated_at
        ) VALUES (
            #{id},
            #{name},
            #{type},
            #{status},
            #{capacity},
            #{allocated},
            #{location},
            #{config, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <!-- Update -->
    <update id="update" parameterType="com.synapsetest.testmanagement.model.ResourcePool">
        UPDATE resource_pools SET
            name = #{name},
            type = #{type},
            status = #{status},
            capacity = #{capacity},
            allocated = #{allocated},
            location = #{location},
            config = #{config, typeHandler=com.synapsetest.testmanagement.config.JsonTypeHandler},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- Delete by ID -->
    <delete id="deleteById">
        DELETE FROM resource_pools WHERE id = #{id}
    </delete>

    <!-- Count -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM resource_pools
    </select>

</mapper>

