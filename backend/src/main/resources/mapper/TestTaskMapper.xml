<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synapsetest.testmanagement.mapper.TestTaskMapper">

    <!-- Result Map for TestTask -->
    <resultMap id="TestTaskResultMap" type="com.synapsetest.testmanagement.model.TestTask">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="environment" column="environment"/>
        <result property="version" column="version"/>
        <result property="testScope" column="test_scope"/>
        <result property="status" column="status"/>
        <result property="priority" column="priority"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="createdBy" column="created_by"/>
    </resultMap>

    <!-- Select by ID -->
    <select id="selectById" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks WHERE id = #{id}
    </select>

    <!-- Select All -->
    <select id="selectAll" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks
    </select>

    <!-- Select by Status -->
    <select id="selectByStatus" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks WHERE status = #{status}
    </select>

    <!-- Select by Environment -->
    <select id="selectByEnvironment" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks WHERE environment = #{environment}
    </select>

    <!-- Select by Created By -->
    <select id="selectByCreatedBy" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks WHERE created_by = #{createdBy}
    </select>

    <!-- Select by Status Order by Priority Desc -->
    <select id="selectByStatusOrderByPriorityDesc" resultMap="TestTaskResultMap">
        SELECT * FROM test_tasks WHERE status = #{status} ORDER BY priority DESC
    </select>

    <!-- Insert -->
    <insert id="insert" parameterType="com.synapsetest.testmanagement.model.TestTask">
        INSERT INTO test_tasks(
            id, name, description, environment, version, test_scope,
            status, priority, created_at, updated_at, created_by
        ) VALUES (
            #{id},
            #{name},
            #{description},
            #{environment},
            #{version},
            #{testScope},
            #{status},
            #{priority},
            #{createdAt},
            #{updatedAt},
            #{createdBy}
        )
    </insert>

    <!-- Update -->
    <update id="update" parameterType="com.synapsetest.testmanagement.model.TestTask">
        UPDATE test_tasks SET
            name = #{name},
            description = #{description},
            environment = #{environment},
            version = #{version},
            test_scope = #{testScope},
            status = #{status},
            priority = #{priority},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- Delete by ID -->
    <delete id="deleteById">
        DELETE FROM test_tasks WHERE id = #{id}
    </delete>

    <!-- Count -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM test_tasks
    </select>

</mapper>

