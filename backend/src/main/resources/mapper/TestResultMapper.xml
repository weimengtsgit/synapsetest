<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synapsetest.testmanagement.mapper.TestResultMapper">

    <!-- Result Map for TestResult -->
    <resultMap id="TestResultResultMap" type="com.synapsetest.testmanagement.model.TestResult">
        <id property="id" column="id"/>
        <result property="reportId" column="report_id"/>
        <result property="testCaseId" column="test_case_id"/>
        <result property="testCaseName" column="test_case_name"/>
        <result property="status" column="status"/>
        <result property="executionTime" column="execution_time"/>
        <result property="error" column="error"/>
        <result property="screenshot" column="screenshot"/>
        <result property="executedAt" column="executed_at"/>
    </resultMap>

    <!-- Insert -->
    <insert id="insert" parameterType="com.synapsetest.testmanagement.model.TestResult">
        INSERT INTO quality_report_test_result (
            report_id, test_case_id, test_case_name, status,
            execution_time, error, screenshot, executed_at
        ) VALUES (
            #{reportId}, #{testCaseId}, #{testCaseName}, #{status},
            #{executionTime}, #{error}, #{screenshot}, #{executedAt}
        )
    </insert>

    <!-- Batch Insert -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO quality_report_test_result (
            report_id, test_case_id, test_case_name, status,
            execution_time, error, screenshot, executed_at
        ) VALUES
        <foreach collection="testResults" item="testResult" separator=",">
            (
                #{testResult.reportId}, #{testResult.testCaseId}, #{testResult.testCaseName}, #{testResult.status},
                #{testResult.executionTime}, #{testResult.error}, #{testResult.screenshot}, #{testResult.executedAt}
            )
        </foreach>
    </insert>

    <!-- Select by Report ID -->
    <select id="selectByReportId" resultMap="TestResultResultMap">
        SELECT * FROM quality_report_test_result 
        WHERE report_id = #{reportId} 
        ORDER BY executed_at DESC
    </select>

    <!-- Select by Report ID and Status -->
    <select id="selectByReportIdAndStatus" resultMap="TestResultResultMap">
        SELECT * FROM quality_report_test_result 
        WHERE report_id = #{reportId} AND status = #{status}
        ORDER BY executed_at DESC
    </select>

    <!-- Update -->
    <update id="update" parameterType="com.synapsetest.testmanagement.model.TestResult">
        UPDATE quality_report_test_result SET 
            test_case_id = #{testCaseId},
            test_case_name = #{testCaseName},
            status = #{status},
            execution_time = #{executionTime},
            error = #{error},
            screenshot = #{screenshot},
            executed_at = #{executedAt}
        WHERE id = #{id}
    </update>

    <!-- Delete by Report ID -->
    <delete id="deleteByReportId">
        DELETE FROM quality_report_test_result WHERE report_id = #{reportId}
    </delete>

    <!-- Delete by Report IDs -->
    <delete id="deleteByReportIds">
        DELETE FROM quality_report_test_result WHERE report_id IN
        <foreach item="reportId" collection="reportIds" open="(" separator="," close=")">
            #{reportId}
        </foreach>
    </delete>
</mapper>